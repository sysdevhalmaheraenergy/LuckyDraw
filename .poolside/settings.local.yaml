tools:
    shell:
        allow:
            - find *
            - head *
            - ls *
            - cat *
            - curl *
            - echo *
            - ssh *
            - grep *
            - cd *
            - npx tsc *
            - npm run *
            - tail *
paths:
    allow:
        - path: /Users/wafa/Library/Application Support/poolside/plans/**
